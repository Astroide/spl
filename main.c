#include <stdio.h>
int main() {
    // int c;
    // FILE *file;
    // file = fopen("main.c", "r");
    // if (file) {
        // while ((c = getc(file)) != EOF)
            // putchar(c);
        // fclose(file);
    // }
    printf("SPL - Some Programming Language");
    return 0;
}